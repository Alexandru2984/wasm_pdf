apiVersion: 1

providers:
  - name: PDF Editor
    orgId: 1
    folder: PDF Editor
    folderUid: pdf-editor
    type: file
    disableDeletion: true
    editable: false
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards

